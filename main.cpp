#include <iostream>
#include <fstream>
#include <string>
#include <omp.h>
#include <math.h>
#include <vector>
// #include "function.h"
#include "PROTOTYPE.h"
using namespace std;
// const double pi = 3.14159;
// class parameter p;
int main()
{
  parameter p;
  vector<int> a;
  vector<double> b;
  a.resize(11,0.0);
  b.resize(7,0.0);
  p.set_param(p);
  p.get_param(p,a,b);
  cout<<"a.ne="<<a[0]<<endl;
  cout<<p.ne<<endl;

}
